# Web Archive Schema Compare

A simple python script to compare current site with old state of a site with waybackmachine.

## Setup

To get started with this project, follow these steps:

### Prerequisites

- Python 3.12
- pip (Python package manager)

### Installation

1. Clone the repository to your local machine:

```sh
git clone git@github.com:deanzaka/web-archive-schema-compare.git
cd web-archive-schema-compare
```

2. Create a virtual environment:

```sh
python3 -m venv venv
```

3. Activate the virtual environment:

```sh
source venv/bin/activate
```

4. Install the required dependencies:

```sh
pip install -r requirements.txt
```

### Running the Project

To run the project, execute the following command:

```sh
python compare_schema.py
```

This script fetches schemas from specified URLs, compares them, and saves the differences to schema_differences.json if any differences are found.

### Deactivating the Virtual Environment

When you're done working on the project, you can deactivate the virtual environment by running:

```sh
deactivate
```

